/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export',
    // その他の設定をここに追加...
      // その他の必要な設定
  reactStrictMode: true,
  swcMinify: true,
  }
  
  export default nextConfig;